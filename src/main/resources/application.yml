server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@//61.73.18.134:1521/MYXE
    username: sys
    password: Akstp3ckd
    hikari:
      pool-name: HikariPool-Oracle
      maximum-pool-size: 5
      connection-test-query: "SELECT 1 FROM DUAL"
      data-source-properties:
        internal_logon: SYSDBA

  # (JPA를 쓸 계획이 없으면 spring.jpa 블록은 과감히 삭제해도 됩니다)
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true

# MyBatis 설정 (spring 블록 바깥, 최상위)
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.server.KH_StudyProjects_WeatherServer.common.favorite.dto

# 로깅 (최상위)
logging:
  level:
    org.hibernate.SQL: debug
    # MyBatis 로그도 보고 싶다면 (선택):
    # com.server.KH_StudyProjects_WeatherServer: debug
    # org.mybatis: debug
